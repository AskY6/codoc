# CoDoc Agent 架构设计

---

## 一、核心问题

CoDoc 中存在两类不同性质的 agent：操作 codoc 本身的 agent（增删改查），和理解特定领域的场景 agent（如 RSS client、摘要、翻译）。本文档定义它们之间的关系、协作协议和产品行为。

---

## 二、两层 Agent 架构

### 2.1 Codoc Agent（基础设施层）

**定位：** 通用的 schema-aware 意图解释器。

**核心能力：** 给定一个 codoc 的 schema + 当前 data + 一段自然语言意图，决定对 data 做什么操作。

- **输入：** `(schema, current_data, natural_language_intent)`
- **输出：** `data_patch`

**关键约束：**

- 不包含任何领域知识。所有领域理解来自场景 agent 产出的意图。
- 通用的——不是每个 codoc 一个实例，而是一个通用 agent 被不同 codoc 的 schema 参数化。
- 拥有对 codoc data 的唯一写入权。场景 agent 不直接写 data，必须通过 codoc agent。

### 2.2 场景 Agent（领域层）

**定位：** 理解特定领域逻辑的决策者。

**核心能力：** 基于领域知识做出判断，读取目标 codoc 的 schema 作为参照，产出面向具体 codoc 的自然语言意图。

**关键约束：**

- 读 schema，不写 data。Schema 是意图构建的参照，不是直接操作的 handle。
- 一个场景 agent 的一次决策可能产出针对多个 codoc 的多条意图。

### 2.3 两层之间的信息流

```
场景 agent 读 schema → 构建自然语言意图 → 意图队列 → codoc agent 读 schema + 意图 → data_patch → propagate
```

生产者-消费者模型。场景 agent 是意图的生产者，codoc agent 是意图的消费者。两者通过意图队列解耦，生产归生产，消费归消费。

---

## 三、Schema 的双重角色

Schema 是两层 agent 共享的契约，在信息流中被读两次：

- **对场景 agent：** 意图构建的参照——读 schema 知道能说什么、往哪个方向说，从而构建更精准的自然语言意图。
- **对 codoc agent：** action space 的边界——决定什么操作合法，将自然语言意图约束在 schema 允许的范围内映射为 data_patch。

---

## 四、意图队列

### 4.1 定位

场景 agent 和 codoc agent 之间的解耦层。

### 4.2 意图结构

每条意图携带：

- **来源：** 哪个场景 agent 产出
- **目标：** 对哪个 codoc（及相关字段）
- **内容：** 自然语言描述的操作语义
- **状态：** 见 §4.4 生命周期

### 4.3 消费侧工程控制

- **debounce：** 短时间内多条意图延迟合并处理
- **merge：** 对同一 codoc 同一字段的多条意图合并
- **rate limit：** 控制单位时间内的执行频率

这些策略由消费侧（codoc agent）管理，生产侧不感知。

### 4.4 意图生命周期

**主流程：**

```
pending → previewed → confirmed → executed → propagated
                    → rejected
                                → failed
```

**异常标记（可叠加在任何状态上）：**

- **conflicted：** 目标字段在意图等待期间被其他操作修改。保留意图，标记冲突，由用户决定是否继续执行。
- **stale：** 意图长时间未审阅，上游数据已发生变更。不自动过期，标记 stale 提示用户。

**失败处理：** 用户 confirm 后 codoc agent 执行失败（schema validation 不通过等），标记 failed，由用户手动处理。不自动重试，不自动回退给场景 agent。

**设计原则：** 系统标记状态，人类做最终决定。不自动丢弃，不自动重试，控制权始终留给用户。

### 4.5 用户界面

意图队列是弱入口——不在主交互路径上，不侵入 chat 体验。

- 角标/badge 提示"有 N 条待处理意图"
- 点开后展开详情，可审阅、确认、拒绝
- 不点不打扰——用户主动拉取，非被动推送

---

## 五、Agent 激活模型

### 5.1 两类激活状态

- **常驻：** codoc agent 永远在线，作为基础设施层随时响应意图。
- **按需激活：** 场景 agent 默认未激活。workspace 中可注册多个场景 agent，用户按需激活。

### 5.2 激活方式

场景 agent 支持两种激活方式：

- **显式选择：** 用户在 agent 列表中手动激活，确定性强。
- **自然语言路由：** 用户在 chat 中输入自然语言，系统自动匹配合适的场景 agent 并激活。

### 5.3 自然语言路由

- **路由成功：** 自动分发给匹配的场景 agent，用户无感。
- **路由失败：** 多匹配、无匹配、模糊三种情况统一处理——回到 chat 中交互式消歧，让用户补充意图或从候选 agent 中选择。

---

## 六、人类审阅粒度

### 6.1 默认行为

所有意图默认需要人类审阅确认后才执行（generate → preview → confirm 流程）。

### 6.2 Trusted Agent 配置

用户可将特定场景 agent 标记为 trusted。Trusted agent 产出的意图跳过人类审阅，直接进入 codoc agent 执行。

适用场景：低风险、高频的自动化操作（如 RSS 抓取写入新 feed item）。

### 6.3 信任层级

- **场景 agent：** 可被用户配置为 trusted 或需审阅。
- **codoc agent：** 始终执行，不需要信任配置——它的信任来自上游（场景 agent 的意图经过审阅，或场景 agent 本身被标记为 trusted）。

---

## 七、整体架构图

```
┌─────────────────────────────────────────────────────┐
│                    用户 (Chat)                       │
│  显式选择 agent / 自然语言输入                         │
└──────────┬──────────────────────┬────────────────────┘
           │                      │
           │ 显式激活              │ 自然语言路由
           │                      │ (失败则交互式消歧)
           ▼                      ▼
┌─────────────────────────────────────────────────────┐
│              场景 Agent（领域层）                      │
│  读 schema → 领域推理 → 产出自然语言意图                │
│  RSS / 摘要 / 翻译 / 数据监控 / ...                   │
└──────────────────────┬──────────────────────────────┘
                       │ 自然语言意图
                       ▼
┌─────────────────────────────────────────────────────┐
│                  意图队列（解耦层）                     │
│  debounce / merge / rate limit                      │
│  状态：pending / conflicted / stale                  │
│  UI：弱入口，角标提示，主动拉取                          │
└──────────────────────┬──────────────────────────────┘
                       │
          ┌────────────┴────────────┐
          │ trusted agent           │ 需审阅
          │ 跳过审阅                 │ preview → confirm/reject
          ▼                         ▼
┌─────────────────────────────────────────────────────┐
│            Codoc Agent（基础设施层）                   │
│  (schema, current_data, intent) → data_patch        │
│  唯一写入权 / 通用 / 无领域知识                         │
└──────────────────────┬──────────────────────────────┘
                       │ data_patch
                       ▼
┌─────────────────────────────────────────────────────┐
│                Codoc (Codata)                        │
│  写入 → 标脏传播 → 下游 force → render               │
└─────────────────────────────────────────────────────┘
```