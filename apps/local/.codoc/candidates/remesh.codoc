---
title: "Remesh"
description: "A CQRS-based DDD framework for large and complex applications"
tags: [candidate, remesh]
data:
  name: "Remesh"
  repo: "remesh-js/remesh"
  stars: 722
  bundle_size_kb: 10
  license: "MIT"
  first_release: "2021"
  features:
    - "CQRS + DDD 架构，天然适合复杂业务建模"
    - "框架无关，官方支持 React / Vue"
    - "RxJS 驱动的副作用管理（可取消、可重试）"
    - "不可变状态 + 增量更新，精准重渲染"
    - "内置 Undo / Redo 时间旅行"
    - "Yjs 协作集成，支持多人实时同步"
  weaknesses:
    - "社区规模极小（~700 stars），生态插件少"
    - "需要同时理解 DDD / CQRS / RxJS，上手门槛高"
    - "运行时依赖 RxJS，实际 bundle 更大"
    - "缺少大规模生产环境验证"
---

# {data.name}

<Badge value={data.stars} label="Stars" color="blue" />
<Badge value={data.bundle_size_kb + "kb"} label="Bundle" color="amber" />
<Badge value={data.license} label="License" color="green" />
<Badge value={data.first_release} label="Since" color="neutral" />

## Features

<Table data={data.features.map((f, i) => ({ "#": i + 1, feature: f }))} />

## Weaknesses

<Table data={data.weaknesses.map((w, i) => ({ "#": i + 1, issue: w }))} />
