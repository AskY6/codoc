// RSS plugin template — re-exports the RSS template from the existing
// templates directory. The template itself stays in templates/rss.ts;
// the plugin just owns the reference.

export { rssTemplate } from "../../templates/rss.js";
