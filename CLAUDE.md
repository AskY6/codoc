# codoc

## design pattern
1. 代数数据类型优先
2. 整洁架构优先

## packages/core 分层约束
- 模块依赖方向严格向内：`dag → codoc`、`cobook → codoc`（只引用 ID 类型），`codoc` 不依赖任何兄弟模块
- `codoc/` 和 `dag/` 不能出现 `workspaceId` 或任何 cobook 概念，租户边界只属于 `cobook/`
- 跨层关联（如 `ThreadCodoc`）放在 `cobook/`，通过 `CodocId` 不透明地引用 codoc
- core 不持有时间戳 / 行元数据（`createdAt`、`updatedAt` 等），这是 storage 层的职责
- ID 一律使用 branded type，编译期防止 `CodocId` / `WorkspaceId` / `NodeId` 混用
- 纯函数返回 `Result<T, E>`，不抛异常；非法状态用 ADT 消除而不是靠运行时校验
- core 零运行时依赖，零 `node:*` / 文件系统 / 网络引用

## 基于 tree 的 context management

面向 AI 协作的文档组织方式。核心理念：把文档组织成一棵**与目录树同构**的层级树，每一层只描述"这一层自己该知道的事"，并持有指向下一层的指针而不是下一层的内容。AI 执行任务时按路径从根**下钻**到相关子树，只把真正用到的叶子加载进上下文。

### 落地规则
- **文件名统一为 `AGENTS.md`**，面向 AI 读者，与 `README.md`（面向人类）区分
- **每个有独立语义的目录放一份 `AGENTS.md`**，严格对齐目录树。新增子模块时同步新增一份 md
- **摘要向上 / 细节向下**：越靠近根越抽象（跨模块契约、全局不变式、子节点索引），越靠近叶越具体（类型清单、函数签名、本模块不变式）
- **单一真相源**：每个事实只在一处定义。子节点不复述父节点的规则，父节点不复述子节点的细节。需要引用时用相对路径链接，例如 `see ../AGENTS.md#layering`
- **显式声明依赖方向**：子节点开头说明"Parent: ..."、"Reads from: ..."、"Must never import from: ..."，让 AI 不读代码就能判断上下文边界
- **不得引入隐式的跨子树耦合**：`cobook/AGENTS.md` 不写 `dag/` 的细节，反之亦然；跨层规则上提到公共父节点

### 何时拆分 / 合并
- 某个子模块 >500 行，或内部出现有独立语义的子目录时，从父节点的对应章节中抽出到新 `AGENTS.md`
- 子模块一直 <100 行且没有子目录时，可以反向并回父节点，避免无内容的空壳 md

### AI 使用方式
- 接到任务先读最近的 `AGENTS.md`，通过索引和依赖声明判断"这个任务落在哪棵子树"
- 只加载路径上的 md（根 + 目标子树），不相关的兄弟子树**完全不进上下文**
- 当跨子树决策时，上溯到最近的公共父节点查找共享契约