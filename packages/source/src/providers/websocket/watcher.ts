// WebSocket watcher: persistent connection with server push.
// Stub — will be implemented when WebSocket source is needed.

export function createWebSocketWatcher(_url: string): void {
  // TODO: implement persistent WebSocket connection for push-based changes
}
