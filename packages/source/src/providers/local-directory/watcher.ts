// Local directory watcher: fs.watch for file additions/removals.
// Stub — will be implemented when directory source is needed.

export function createLocalDirectoryWatcher(_path: string): void {
  // TODO: implement fs.watch-based directory change detection
}
