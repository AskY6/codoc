// RSS watcher: periodic polling for new feed items.
// Stub — will be implemented when RSS source is needed.

export function createRssWatcher(_url: string, _intervalMs?: number): void {
  // TODO: implement polling-based RSS feed change detection
}
